<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.4/Chart.js"></script>
    <script>
        function do_delete(val){
            const conf = window.confirm("Want to activate this user ?");
            if(conf === true){
                fetch('http://localhost:8000/visitorStatusChange', {
                    method: 'POST', // or 'PUT'
                    headers: {'Content-Type': 'multipart/form-data'},
                    body: JSON.stringify({"email": val, "isDelete": true}),
                })
                .then((response) => response.json())
                .then((data) => {
                    window.location.reload();
                })
                .catch((error) => {
                    alert("Something went wrong, try again !!!");
                });
            }
        }
    </script>
</head>
<body>
    {{>adminNavbar}}
    <div class="container">
        <div style="margin-top: 24px;">
            <form action="?" method="POST">
                <div class="mt-4 mb-3">
                    <label for="Name" class="form-label">Library name</label>
                    <input type="text" class="form-control" id="Name" name="name" placeholder="Type library name">
                </div>
                <div class="mt-4 mb-3">
                    <label for="uid" class="form-label">Library Unique ID</label>
                    <input type="text" class="form-control" id="uid" name="uid" placeholder="Type a unique id">
                </div>
                <div class="mt-4 mb-3">
                    <label for="manager" class="form-label">Manager Email</label>
                    <input type="text" class="form-control" id="manager" name="manager" placeholder="Type manager email id">
                </div>
                <div class="mb-3">
                    <label for="libAddress" class="form-label">Library Location</label>
                    <textarea class="form-control" name="address" id="libAddress" rows="3"></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-u1OknCvxWvY5kfmNBILK2hRnQC3Pr17a+RTT6rIHI7NnikvbZlHgTPOOmMi466C8" crossorigin="anonymous"></script>
</body>
</html>
